spring:
  application:
    name: movierflix

  datasource:
    url: jdbc:postgresql://localhost:5432/movierflix
    username: docker
    password: docker
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true